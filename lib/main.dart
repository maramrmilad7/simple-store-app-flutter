import 'package:flutter/material.dart';

void main() {
  runApp(const StoreApplication());
}

class StoreApplication extends StatelessWidget {
  const StoreApplication({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
